{
    "name": "415-5-demo",
    "version": "0.1.0",
    "scripts": {
        "watch:eleventy": "npx @11ty/eleventy --serve",
        "watch:sass": "npx sass sass:_site/css --watch",
        "start": "npm run watch:eleventy & np, run watch:sass"
    },
    "devDependencies": {
        "grunt": "^1.6.1",
        "grunt-contrib-sass": "^2.0.0"
    }
}
